# === requirements_improved.txt: Aktualisierte Abhängigkeiten für verbesserte UI ===

# Solana-Bibliotheken (Original)
solana>=0.32.0,<0.33.0
solders>=0.19.0,<0.20.0
spl-token>=0.6.0,<0.7.0
mpl-token-metadata>=0.2.0,<0.3.0

# UI-Bibliotheken (Neu hinzugefügt)
customtkinter>=5.2.0
darkdetect>=0.8.0

# WebSocket-Unterstützung für Monitor
websockets>=11.0.0

# Optional: Für erweiterte Funktionen
pillow>=9.0.0  # Für Bildverarbeitung in UI

